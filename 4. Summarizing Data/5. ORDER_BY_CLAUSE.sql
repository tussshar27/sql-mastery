SELECT first_name, last_name, salary
FROM employees
ORDER BY salary
-- ORDER BY clause is used to sort rows in ascending or descending order using specific column(s).
-- by default, its asc order, DESC keyword is used to convert into descending order.
-- eg. ORDER BY salary DESC;
